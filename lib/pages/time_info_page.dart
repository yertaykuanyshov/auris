import 'package:flutter/material.dart';

class TimeInfoPage extends StatelessWidget {
  const TimeInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("OK"),
    );
  }
}
