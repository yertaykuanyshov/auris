name: auris
description: "Listening time tracker"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '3.1.2'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
